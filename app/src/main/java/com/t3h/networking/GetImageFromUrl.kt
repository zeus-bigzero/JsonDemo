package com.t3h.networking

import android.os.AsyncTask

class GetImageFromUrl : AsyncTask<String, Unit, ByteArray>() {

    override fun doInBackground(vararg p0: String?): ByteArray {
        return byteArrayOf()
    }
}